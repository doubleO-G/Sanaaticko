Check up on sail dockerization
    run composer
    composer install
    then sail up ...... #for local only with sail not docker
    # docker-compose-sail.yml file is where its at

    copy .env before building

    import sql 

    sudo docker exec -it ticketsapp sh
    
    composer install
    
    chown -R www-data:www-data /var/www/html/storage
    chown -R www-data:www-data /var/www/html/bootstrap
    chown -R www-data:www-data /var/www/html/public

    chmod -R 775 /var/www/html/storage
    chmod -R 775 /var/www/html/bootstrap
    chmod -R 775 /var/www/html/public

    php artisan key:generate



payments gateway integration
- pesapal
- safaricom


git config --global user.name "Tony"
git config --global user.email "jayton006@gmail.com"